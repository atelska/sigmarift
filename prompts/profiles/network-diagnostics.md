Focus on network connectivity, DNS, routing, listening sockets, firewall state,
and service reachability. Test the path layer by layer with narrow commands and
separate local, host, network, and remote-service failures. Avoid broad packet
capture unless necessary, and summarize the evidence supporting the diagnosis.
